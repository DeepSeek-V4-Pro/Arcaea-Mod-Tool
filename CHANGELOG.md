# 更新日志

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-08-18

**首个正式版本。** 完整功能与稳定性修复，可正常完成「扫描 → 替换 → 构建 → 安装」全流程。

### 新增功能

- **素材浏览**
  - 中央目录解析秒级扫描，17 个分类 + 数量统计，分类名经 APK 实测校准
  - 角色分组视图（自动读取 characters.json 角色名）、搜索、排序、分页、键盘翻页
  - 缩略图磁盘缓存 + ETag 条件请求（透明区白底）
- **素材替换**
  - 拖拽 / 点击替换，三栏实时预览（原素材 / 你的素材 / 处理后预览）
  - 图片处理：自动裁切校准（比例适配+保留位置选择）、缩放、拉伸保持原尺寸、PNG/JPG 转换、质量调节
  - 文本类素材在线编辑、音频直接替换
- **智能图片适配**（按素材类型自动识别，无需手动设置）
  - 头像（`*_icon.png`）：自动菱形蒙版，匹配官方镂空形状
  - 联机立绘（`*_mp.png`）：自动半身裁切（实测上部 55%）+ 截断处渐变淡出（对照官方 alpha 曲线）
  - 普通素材：自动按原素材比例裁切校准
- **替换清单管理**
  - 解包/替换双页控制台；补丁启用/停用（停用不参与构建）
  - 补丁包导出/导入（zip，便于备份分享）
  - 批量勾选导出（跨页勾选、全选本页、导出选中/全部）
- **构建与安装**
  - 原始字节级重打包（只重压缩被替换条目，1.8GB 包分钟级完成）
  - 纯 Python APK v2 签名（算法 0x0103 PKCS1-SHA256）+ 构建后三道自校验
  - 进度/日志轮询、产物下载
- **体验与易用性**
  - `input/` 原包目录自动识别（放入即用，无需配置路径）
  - `start.bat` 自愈式一键启动：自动创建 venv、装依赖（国内镜像优先）、构建前端、端口检测、就绪后自动开浏览器
  - 全新 UI：设计令牌化布局、四级响应式断点（≤960px 侧边栏变横向分类条）、自定义下拉组件
  - 配置弹窗、全局提示、键盘快捷键

### 修复

- **签名算法 ID 错误**：0x0101 误用（实为 PSS）导致设备报"缺少开发者证书"，修正为 0x0103
- **内容摘要构造错误**：顶层摘要块数写死 1 且预哈希，与设备计算不一致；修正为 AOSP 标准（0x5a ‖ 真实块数 ‖ 块摘要拼接），并以官方 apksig jar 逐字节验证
- **"保持原始尺寸"失效**：原实现取的是替换图自身尺寸（等于没处理），改为从 APK 读取原素材真实尺寸并缓存
- **图片处理对话框布局错乱**：复选框被 `width:100%` 撑坏，重构 `set-row`/`set` 布局体系
- **下拉弹层位置偏移**：面板 `backdrop-filter` 改变 `fixed` 包含块导致弹层偏移，改为 Teleport 到 body
- **下拉弹层点击即关**：`<label>` 将点击重派发给隐藏 select 导致刚打开就关闭，触发器忽略合成点击
- **脚本编码/行尾问题**：install/start 脚本重写（ASCII 保证跨区域可用、CRLF、块内括号转义），移除自动覆盖 start.bat 的旧逻辑
- **分类误判**：`img/story/actselect` 误入"剧情 CG"；音符/轨道皮肤散件混入"其他界面"——按实测数据修正规则

### 开发期（0.x 历史摘要）

- Vue 3 + Vite 前端重写（组件化、图片替换流程重构）
- 素材导出提速（按偏移顺序单次读取，5760 张约 7 秒）
- 缩略图缓存方案迭代（ETag + 版本参数，解决浏览器缓存不刷新）
- 解包/替换双页控制台、补丁启停、导出/下载 API

---

### 版本规划（未发布）

- [ ] 深色主题
- [ ] 更多形状蒙版（圆角 UI 等，按需）
- [ ] 多语言界面（EN）
- [ ] 批量替换向导（同尺寸素材一键套用）
